# 众安 UED 内部分享 · 逐字稿备份

分享人：斯琴托娅
场合：UED 4 组 · 内部交流
题目：AI驱动下的设计工程化与生产范式变革

- 页数：52
- 逐字稿字数（去空白）：7372
- 已改页：25（Chrome 里用 file:// 打开时，右侧讲稿面板改过并保存在本地）
- 未改页：27（网页上显示的仍是 HTML 内置稿）

---

## 开场

### 01　封面
1 min `s0`

大家好，我是斯琴托娅。

今天跟大家分享的主题是《D20：AI 驱动下的设计工程化与生产范式变革》。

前面大家听到的一些ai的分享，都是一些ai的概念和流程，这次的分享，是教你手把手做一套系统。

这是大麦 UED 历时 14 个月，搭建完成一套面向 B 端设计、现已稳定落地运行的完整系统。依托这套体系，仅需输入一句话需求，或是一份 PRD 文档，就能实现从设计到可上线产物的自动化交付。

> TIP　副标题不用念。停在『问题一层层递进』，翻议程。

### 02　今天怎么听
1.5 min `s-agenda`

这次会站在一起三个分享人的视角分享，也就是第一人陈表述，和大家聊聊本次 D20的分享

看人：AI 重构设计工作，痛点卡点在哪？设计师的角色认知与能力体系该如何迭代；

看路径：14 个月三轮迭代跃迁，交付效率从四天半优化至一个半小时，理性探讨自动化的真实边界；

看系统与方法：平台落地实践、五大架构搭建方案，复盘建设难点，沉淀可复用的实践经验。

> TIP　左中右三张卡。收在底部『问题 → 路径 → 系统 → 方法』。

## 问题

### 03　AI 时代，设计是否变得简单了？
0.5 min `sy-easy`

AI 工具普及之后，很多人会思考：AI 时代，设计是否变得简单了？

想要找到答案，我们先复盘 B 端设计十年的演进历程。

> TIP　只念标题，停一两秒再翻。

### 04　设计系统无法被交付
1.5 min `sy-undeliver`

一路走来，刚好经历三个阶段：

2014 高保真 + 注解；2020 Figma+Token，搭建组件化设计系统；2026 走向需求意图 + 设计 Agent。

十年来我们始终在追求设计工程化，但有一个痛点长期存在：过往最终交付物始终是图纸。

图纸移交开发的瞬间，链路就出现断裂。

也正因如此，传统设计系统仅仅是一套静态规则，没办法实现端到端自动化交付。

> TIP　三张年卡从左到右。收到绿字，停一秒再翻。

### 05　AI + 设计的四个瓶颈
1 min `sy-bottleneck`

大家以为接入 AI 就能减负，但真实感受恰恰相反：引入 AI，事情并没有变得简单。

我们在落地过程中直面四大卡点：

设计系统适配难题、需求理解精度不足、不能直接交付下游、数据安全存在隐患。

多重问题交织在一起，倒逼整个设计团队重新定位自身价值。

外部链路变革之前，先看内部：设计师的角色正在如何重构。

> TIP　四块从左到右点过，不要展开成我们的对照。

### 06　角色怎么变？
1.5 min `sy3`

四大瓶颈横亘在前，破局先看人。
现在的设计师都可做到的是：
AI Designer：借助 AI 加速绘图，核心价值局限于产出图纸；
我们想要实现自动化，要达到的是：AI Design Engineer：不止追求快速出图，重点在于定义规则、搭建自动化设计产线。
只会用 AI 画图不算错，但只是起点。
当 AI 可以自主生成界面，绘图工作会逐步被承接；未来更高价值的能力，是搭建体系、制定规则。

> TIP　先左后右。收在『定规则』。

### 07　认知怎么变？
1.5 min `sy4`

角色变了，更底层的是认知彻底变了。

过去我们做设计规范，是做给人看的。

所有组件规则、字号间距、按钮状态、业务红线，全部沉淀在 Figma、文档、经验里。

靠人翻稿、靠人记忆、靠人对齐。

但只要换一个页面、换一个需求、换一次生成，AI 就读不到、记不住，规范直接失效。

所以新时代的设计认知，只有一句话：规范要能被 AI 读。

我们不再只是把规范做全、做细，而是要把所有设计约束、业务约束，全部固化成机器可识别、可调用、可强制执行的规则文件。

> TIP　顺着从 → 到。最后一句只口播。

### 08　能力要补哪几块？
2 min `sy5`

认知升级之后，设计师需要补齐五项核心新能力，大家可以对照自查。

第一，设计系统拆解。把 Token、组件、范式、模板分层梳理，做到每一层都能被 AI 独立索引和调用。

第二，结构化布局。放弃自由画布，全面用 Auto Layout、Flexbox 标准化布局，保证 AI 产出稳定可控。

第三，AI Coding 能力。具备代码思维，能搭建工具链路、驱动设计产出落地。

第四，Agent 编排能力。理解需求意图、上下文挂载、多 Agent 协同，会编排 AI 生产流程。

第五，AI 资产治理。把零散业务经验，沉淀为可复用、可被 AI 调用的标准化资产。

至此，人的角色、认知、能力三层变革全部讲完。

> TIP　上三下二扫过。收在『下面看路』。

## 路径

### 09　三次生产力跃迁
2 min `fm5`

人的变革说完，我们进入第二部分：智能ai系统14 个月经历的三次迭代跃迁。
这三次迭代不是并列功能，是层层补位、逐级破局的成长过程。
第一阶段，我们先做 A2D。
优先解决个体出图效率，让设计师单点提速，但这个阶段只能赋能个人，没法全局规模化。
针对这个短板，我们迭代第二阶段 D2C。
让 AI 真正读懂设计稿，彻底干掉重复还原、视觉返工的问题，把设计到代码的交付质量稳住。
在前两步的基础上，我们最终完成完整闭环 P2D2C。
把需求到设计、设计到代码的全链路彻底打通，实现端到端自动化生产。
整个过程，每一步都在解决上一步的遗留问题：先跑通单点，再放大全局。
接下来，我们看这三轮迭代跑出的真实落地结果。

> TIP　ACT 1 到 3 递进，不是并列。点完 ACT 3 再翻。

### 10　效率对比
1.5 min `fm3`

这是大麦14 个月三轮迭代跑通后的真实落地数据。

以一套6 个页面的 CRM 标准需求为例：

传统交付模式，整体需要四天半完成；

通过我们 P2D2C 全新工作流，全程只需要一个半小时。

> TIP　左右对照。指一下右侧走查『省略』。不要讲成不用设计师。

### 11　双链路
1.5 min `fm10`

我们看到了 1.5 小时的真实效率提升，但必须回归落地真实图景，AI 设计不是百分百全自动的神话。
我们目前真实的生产状态是六四开：
占业务量最大头的列表、表单、看板这类标准页面，大概六成场景，已经实现全自动产出。
而营销页面、复杂交互、个性化场景这四成内容，依然由设计师先创意、先定义，再转码落地。
真正的工程化胜利，不是机器替代所有人。
而是把量大、重复、标准的页面交给自动化产线，把人力彻底释放出来，专注攻克高复杂、高价值、高创意的场景。

> TIP　点 60% 和 40%。收在『这就已经是胜利』。

## 平台

### 12　开启设计的自动驾驶时代
0.5 min `d-platform`

下面我们要进行本次分享最重的部分：如何构建-智能体驱动的设计新交付模式。

> TIP　两行主标题慢读。翻到前置验证。

### 13　前置自建验证
1.5 min `lyj-pre`

正式工程之前，设计侧先自己验证流程和技术可行性。

可以按照左边七件事按顺序检查一下：自然语言或文档能丢进去；能跟人澄清；规范以 Token 和组件注入；沙盒里是能点的真 UI；思维链可审查；四种预览方便交付；生成之后还能多轮改。

核心命题就一句：大模型吃自然语言或 PRD，吐出符合内部规范的 B 端代码化设计。

> TIP　先扫左七条，再落右『核心命题』。

### 14　从核心目标出发
1.5 min `lyj-goal`

功能不是先列清单。每个功能背后都有一个具体问题。

ai不懂规范，做知识层。需求会模糊，做理解层。生成了要看得见，做渲染层。要交给开发，做交付层。一次不满意，做迭代层。不能只是本地 Demo，做产品化。

先想清楚每层的核心问题，功能边界自然清晰。

> TIP　先指核心命题，六行从上往下。

### 15　平台怎么用
2 min `lyj1b`

首先我们直观体验下整套系统如何使用。

点击左侧步骤，右侧同步展示对应界面。

平台支持两种输入方式，a2d和p2d。我先举个例子，直接输入 “生成一个管理页面”，系统会先输出需求理解摘要，再按优先级发起澄清提问。

等双方通过需求确认书完成对齐后，才开始生成页面。

产出支持四种预览模式：单页、画板、交互标注、开发视图。

如果效果不符合预期，可以直接继续提出修改意见，系统保留当前画布，仅做局部调整，不用全部推倒重来。

> TIP　点步骤切图。讲到四种预览时点右下小图过一遍。

### 16　先想清楚再写代码
2 min `lyj-think`

看完产品演示，接下来我们深入底层实现思路。整套平台遵循一个原则：先理清核心架构，再启动开发。
我们先明确核心命题：依靠自然语言、PRD 作为输入，直接产出符合大麦 B 端设计规范、能够交付使用的设计稿。
围绕这个目标，我们推导出五层关键设计，自上而下贯穿整套系统，接下来我会逐层拆解。
第一层是知识注入系统，也就是知识层，目标是让 AI 吃透大麦 B 端完整设计规范；
第二层需求澄清交互，属于理解层，在生成设计前完成需求对齐，消除所有模糊信息；
第三层输出协议设计，协议层依靠结构化 JSON 合约，搭配多层兜底解析，保证输出稳定可控；
第四层多种渲染引擎，渲染层提供两种模式：JSON 精确模式和 JSX 灵活模式，适配不同生成场景；
第五层多轮迭代编辑，迭代层支持上下文持续感知，通过多轮对话不断优化设计。
这里分享一个核心认知：系统最终能做到什么水平，很大程度取决于它能不能准确理解需求。先把问题思考透彻再动手开发，才能事半功倍。

> TIP　先指左边命题和右边认知，再扫 01 到 05。翻到 Key 1。

### 17　知识注入系统
1 min `lyj-inject`

Key 1。大模型不懂你们的 B 端规范，得主动喂。

八个文件按优先级注入 System Prompt：核心指令、设计系统、样式、组件映射、Token，再往下是列表、表单、看板。

> TIP　按 01 到 08 扫过。强调优先级。

### 18　知识怎么喂、怎么检索
1.5 min `lyj-routes`

摆在底层最核心的难题：通用大模型并不熟悉大麦 B 端设计规范。我们要解决两件事：设计知识怎么灌入系统，灌入之后如何检索调用。

我们对比了多种方案，没有一开始就选用复杂路线，采用分阶段演进策略。

左边是 Registry 路由方案：好比给 AI 一份目录手册，AI 根据路由表，自主找到并读取完整规范文件。

优势是拿到完整规则，不会出现内容被截断；支持后台在线配置、启停规则，修改无需重启服务，架构轻量化，适合前期 MVP 快速跑通链路。

右边是 RAG 检索方案：相当于安排图书管理员，收到需求后先做语义检索，提取相关片段送入提示词。

但它存在明显短板：文档切片很容易把一条完整规范切断，信息残缺，导致 AI 生成出错。

所以我们的落地路径很清晰：前期先用 Registry 方案稳住链路，等系统成熟后，再叠加 RAG 支撑更大规模知识库。

解决了知识投喂这个底层问题，接下来我们来看整套 平台完整的架构体系。

> TIP　左右对照。强调分阶段，不要站队。

### 19　同一套知识底座
1 min `lyj-base`

刚刚我们聊了 Registry 和 RAG 两条技术路线，大家不用纠结选哪一条。
两条路线，底层共用同一套知识底座。哪怕后续技术方案切换，已经沉淀的设计规则完全可以复用，现在启动建设，投入不会白费。
这套 Design Rules 分成五层，大家留意：序号不等于建设顺序，我们按照落地优先级推进。
现阶段优先完善三块：强约束底线规则、全局交互规范、组件使用规则；
典型页面 JSON Schema 还在持续搭建；面向场景的业务定制规则，目前先做规划储备。
知识底座的建设思路就介绍到这里，接下来进入第二个核心模块：需求澄清。

> TIP　五条往下指。收在『现在做都不会浪费』。

### 20　AI 并不真正理解业务上下文
1.5 min `lyj-k2ctx`

第二个关键点：需求澄清。

AI 没办法真正读懂业务上下文。不管是自然语言描述还是 PRD 文档，信息本身经常存在缺失、模糊。

我们对比两种模式：

传统产设协作，产品给到设计师之后，遇到模糊点设计师会主动追问、提前对齐，信息补齐之后才开始设计。

但如果 AI 平台缺少澄清环节，输入需求就直接生成，遇到信息缺口 AI 会自行脑补，很容易产出偏差，带来大量返工。

这里我们得到一个核心结论：

在产设研流程里，需求对齐是开工前必不可少的一步。AI 自动化平台不能跳过这个环节，只是我们要用机器可运行的机制，把人工对齐转化成系统化流程。

> TIP　先左后右。收在底部洞察。

### 21　需求澄清 Agent
1.5 min `lyj-k2agent`

我们的解决方案就是需求澄清 Agent，先对齐，再动手。

整套流程很清晰：平台接收需求之后，澄清 Agent 主动识别需求里的模糊点，通过结构化选项向用户提问，用户直接选择确认，信息全部对齐之后，才开始生成设计。

底层依靠三点支撑：

依靠 Prompt 约束 AI 该关注哪些问题；结合业务知识库，保证问到业务关键点；交互上采用可选点击形式，不用用户大段文字输入，降低沟通成本。

日常主要澄清这些内容：页面类型、数据规模、业务状态流转、交互与权限控制。

经过这一轮对齐，AI 最终拿到没有歧义的完整需求，从源头减少返工。

> TIP　先指五步，再扫底层和效果。

### 22　两类信息缺口
1.5 min `lyj-k2gap`

在大模型需求理解的环节里，需求里的信息缺口分成两类，二者的解决路径不能混淆。

第一类，上下文缺口：平台现状、通用业务规则，信息客观存在，只是 AI 不掌握。这类问题靠知识库注入补齐，不应该反复询问使用者。

第二类，规格缺口：需求文档没有写明，只有提需求的人才能确定，这类内容才通过澄清对话交互补齐。

这里有一条关键判断标准：

如果 Agent 一直在反复询问 “页面应该是什么样式”，本质是知识库建设不完善，并不是澄清对话设计出了问题。分清两类缺口，才能避免无效提问，让整个流程更加高效。

> TIP　左右对照。收在关键判断。

### 23　完整交互流程
2 min `lyj-k2flow`

理清两类信息缺口之后，我们来看完整交互流程。整套流程必须严格顺序执行：不能跳过摘要直接提问，也不能无脑套用通用模板。

Step0 用户提交需求，上传 PRD 即可；

Step1 Agent 自动读取文档，提取信息，结合知识库形成初步理解；

Step2 先输出需求理解摘要，标出不确定点，让用户先纠错、再回答问题，避免答非所问；

Step3 生成澄清清单，问题区分关键、重要、次要分级，优先解决影响方案的核心问题；

Step4 吸收回答，最多两轮追问，信息充足就停止；

Step5 输出十项结构化的需求理解确认书，「本次不包含」必须填写，控制需求范围蔓延；

Step6 自动沉淀本次对话产生的新知识，给出归档建议；临时上传的截图只做上下文参考，不存入知识库。

这套流程保证澄清有理有据、循序渐进，不会出现漫无目的的反复提问。

> TIP　强调先摘要再提问，和『本次不包含』。

### 24　问 vs 不问
1 min `lyj-k2ask`

有了完整流程，我们再明确一套提问判断标准。核心思路：问题要精准，宁可放过小细节，也不要堆一堆问题给用户。

先说要问：

会影响页面布局、交互结构的信息；文档存在歧义；存在多种方案，需要用户选择。

下面这些不要问：

知识库能够自动推断的内容；字号、颜色这类规范内的视觉细节；技术实现细节。

记住一条黄金法则：

如果一个问题不会改变核心设计方案，那就不问。

同时我们限定索要截图的场景，只在三种情况需要：

参考现有页面无法确认样式、改造范围要视觉确认、用户描述的状态存在歧义。

依靠这套规则过滤无效提问，让澄清对话聚焦重点。

> TIP　先左右，再念黄金法则。

### 25　落地路线图
1 min `lyj-k2road`

Prompt 加知识库先独立验证，稳定了再投工程。

第一周冷启动文档和高频模块。第二周拿真实 PRD 零代码验证。第三四周才进平台：问题卡片、截图入口、确认书。长期做沉淀 review。

> TIP　四段顺着指。停在『稳定后再投工程』。

### 26　输出协议设计
1.5 min `lyj-k3over`

需求澄清讲完了，到第三个关键点输出协议。三个问题事先想清楚：输出什么、什么格式、怎么解析。

大家可以看看左边八字段 JSON 合约，必须按这个吐。右边三层兜底：严格 parse、逐字段提取、代码块里捞。AI 的输出永远不是百分之百规则的，解析器鲁棒性决定稳不稳。

> TIP　左合约右解析。『鲁棒性』可点开讲。

### 27　输出是给机器解析的
1.5 min `lyj-k3why`

输出不是给人看的文本，是要被机器解析、渲染成真实 UI。

自由文本和 Markdown 渲染不了、带不上标注。JSON 合约字段固定，思维链、说明书、标注分开，还能切渲染模式。

> TIP　左右对照。收在『协议质量决定体验』。

### 28　8 字段输出合约
1.5 min `lyj-k3fields`

每一次输出守这八个顶层字段，前端按字段分流。

thinking 是思维链。summary 给人看的气泡。selected_components 方便校验用了哪些组件。render_mode 决定走 JSON 还是 React 沙箱。code 和 spec 互斥。annotations 给 Inspect。pages 管多页。

> TIP　从上往下指字段名，不要念代码。

### 29　3 层兜底解析器
1 min `lyj-k3parse`

即便 Prompt 写得再严，也可能有尾部逗号、被 markdown 包住。必须逐层兜底。

先严格 parse。过不去就按字段捞。再不行从代码块里抽。宁可降级，也不给人看空白。

> TIP　三层从上往下。收在宁可降级。

### 30　标注协议与替代路径
1.5 min `lyj-k3ann`

协议里还有两件事。一是交互标注：关键元素打上 id，Inspect 一对就高亮，交付时说明贴在 UI 上。

二是需求糊的时候不硬生成：可以吐澄清问题，也可以把生成流停住，收完答案再继续。协议不是死格式，是一棵决策树。

> TIP　先标注，再改道。收在决策树。

### 31　多渲染引擎
1.5 min `lyj-k4over`

Key 4。一套渲染不够用。

标准列表表单走 JSON，组件直接映射，追求精确还原。复杂交互走 JSX，浏览器里即时编译。选哪套由 AI 看 render_mode，不用人手动切。

> TIP　左右对照。收在『AI 自己选』。

### 32　一套引擎不够用
1 min `lyj-k4dual`

B 端从标准页到自由探索，深浅差很多。单引擎要么死板，要么跑偏。所以双引擎并行，不行就降级。细节扫过即可，下一页看容错。

> TIP　比上一页更细，带过。翻到降级。

### 33　优雅降级策略
1 min `lyj-k4fall`

容错是第一优先级。用户不该因为 AI 选错模式看到白屏。

先 JSON，失败自动切到代码引擎。人无感。无论它吐什么，看到的永远是可交互的结果，不是报错页。

> TIP　四步往下。收在铁律。

### 34　三种交互模式
1 min `lyj-k4mode`

同一张生成页，角色不同，看法不同。Preview 是纯净预览。Inspect 看交互说明。Dev 看 CSS 和 Token，给开发。

> TIP　三模式扫过。和前面四种预览呼应。

### 35　多轮迭代编辑
2 min `lyj-k5over`

Key 5。让 AI 记得上次生成了什么。

没有上下文，你说把按钮改蓝，它整页重做。注入当前画布之后，只改该改的，其余不动。从生成工具变成协作伙伴，关键就这一步。

> TIP　先左右对比。不要念代码。

### 36　从一次性生成到协作伙伴
1 min `lyj-k5why`

单轮生成只能出 Demo，真能用要多轮打磨。

没上下文会整页重来。对话一刷新就没了。一个产品几十页、每页多轮，没有组织和检索会串台。下一页用三层模型接住。

> TIP　三张卡从左到右。

### 37　数据架构三层模型
1 min `lyj-k5data`

Project 是一个产品。Page 是其中一页。Conversation 才是这一页上的一次设计会话，消息、当前代码、渲染模式都在这里。按页面隔离，需求不会串。

> TIP　三层从上往下。停在 Conversation。

### 38　上下文静默注入
1.5 min `lyj-k5inject`

用户只输入修改指令。平台在幕后把当前画布包进去，人无感知。

只在 Code 模式且已有代码时注入。JSON 模式不注入，免得它看见 JSX 就锁死。空画布也不注入。注入错误上下文，不如不注入。

> TIP　三个场景。收在『不如不注入』。不要念代码。

### 39　对话历史管理策略
1 min `lyj-k5hist`

传给模型只留最近十六条，兼顾连贯和 Token。入库前洗掉临时字段。对话名默认取第一句话。旧会话可以按名找回完整状态。

> TIP　四张卡扫过。停在十六条和一键恢复。

### 40　关键设计决策 Why
1.5 min `lyj-k5dec`

按页面隔离对话，混在一起会乱。只在 Code 模式注入，是为了保护渲染模式。截十六条，是因为知识库已经占了窗口。

多轮迭代的本质是状态持续性：画布、对话、渲染模式，三者同步保存、同步恢复。

> TIP　五条 Why 往下。收在状态持续性。

### 41　系统架构全景
1.5 min `lyj-pano`

一次请求怎么跑：入口、澄清、知识组装、推理、协议解析、渲染。底下是知识资产、状态、服务端、双引擎路由。边界清晰，每一段都可以独立升级。

> TIP　先上六步，再扫底下四块。

### 42　技术栈全景
1 min `lyj-stack`

四层选型都对着那一层的核心问题，不是随机堆。前端管三层数据，推理层流式加双协议，渲染层双引擎加校验，服务端登录、库、部署。扫过即可。

> TIP　四列从左到右。不要展开成技术课。

### 43　坑 01 假 JSON
1 min `lyj-pit1`

你让它返回 JSON，经常是假的：被 markdown 包着、带注释、尾巴多逗号、截断。直接 parse 会崩，但又不能不用 JSON。解法就是前面那三层解析。不要假设它会完美守约。

> TIP　先左现象后右解法。

### 44　坑 02 未验证的假设
1.5 min `lyj-pit2`

最贵的 bug 往往来自最早的假设。以为它一定按协议吐、一定认识组件名、喂进去就等于看懂了、多轮上下文会自动留住。每一条都要用真实数据验。感觉对不等于验证过。

> TIP　先念左边假设，再落右边动作。

### 45　坑 03 AI 失忆
1.5 min `lyj-pit3`

对话越长，最初的约定会被悄悄忘掉。不是它变笨，是规则信号被稀释了。

功能做完就开新对话。规则写进文件，每次自动读。发现偏移立刻重申。AI 没有真正的记忆，记忆来自上下文。

> TIP　先症状再习惯。收在『要规则化』。

## 方法

### 46　可复用的经验
2 min `lyj-reuse`

从这套项目里收六条，搭 Agent 都能用。

架构先于代码，不然只是更快走错路。领域知识才是壁垒。协议决定稳不稳。小步验证。规则写进文件。最后一条：你是决策者，AI 是执行者。产品形态、体验、优先级，必须人来拍。

> TIP　六张卡从上到下。收在『你是决策者』。

### 47　五条黄金法则
2 min `lyj-gold`

日常跟 AI 协作就盯这五条。先想后做：让它列方案，你选再写。极简：只写当下需要的。外科手术：只改该改的。定义完成：先说验收长什么样。持续验证：每做一步就测。

绿的是可以照着说的指令。自信和正确，不是一回事。

> TIP　五条往下。绿句稍停。

### 48　工具触发的 4 种底层模式
1.5 min `lyj-trig`

工具怎么被激活，就这四种：自动预加载、语义触发、描述匹配、手动调用。

任务描述越精确，触发越稳。「写个登录」可能什么都不触发；「实现带 JWT 认证的登录接口」就能打中安全审查和 API 设计。

> TIP　四张卡扫过。收在底部洞察。

### 49　分层工具架构思维
1.5 min `lyj-tlayer`

不管用哪套工具，都可以按这五层来组织。L0 行为守卫全程在。L1 从规划到发布。L2 按任务激活专项能力。L3 把 MCP、知识库按需挂上。L4 交付前的质量关卡。

这套编号和前面 Design Rules 不是同一套，不要混着看。

> TIP　五层从上往下。提醒编号不要混。

### 50　3 个致命误区
2 min `lyj-fatal`

这三个不是新手才会踩。

一次性倾倒所有需求，拆成每步一个目标。不验证就信任，它说完成了也要跑、要看。一个对话里塞太多任务会串台，切换就开新对话。

信息量不等于有效性。自信不等于正确。上下文被无关信息填满就降智。

> TIP　三张卡。每张先误区再对策。

### 51　底层逻辑总结
1.5 min `lyj-logic`

收成三条。工具在变，逻辑不变。你的价值不是会用工具，而是知道要它做什么、能判断做得对不对。你负责方向和判断，它负责速度和执行。

AI 时代更需要放大的是判断力。能定义问题、拆解任务、验证结果的人，无论工具怎么迭代，总能更大受益。

> TIP　三条往下。『真相』那句放慢。

### 52　感谢聆听
0.5 min `s-thanks`

原本三个多小时的大麦完整方案，受时间约束，今天只带来核心干货。这套体系我当时听完，也消化了很久才理顺。

建议大家后续可以尝试在工作里落地这套流程。

我的分享就到这里，有疑问我们接下来交流。

下面是浩然的分享

> TIP　停住，留提问。
